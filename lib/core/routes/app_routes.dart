
import 'package:flutter/material.dart';

Map<String, WidgetBuilder> routes = {
  // Routes.MODE_SELECT_VIEW: (_) => ModeSelectView(),


};

class Routes {
  static const MODE_SELECT_VIEW = '/mode-select';
 
}
